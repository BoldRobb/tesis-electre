# Variables de entorno para Railway
# Copia estas variables en la pestaña "Variables" de tu servicio en Railway

# MySQL - Railway provee estas automáticamente cuando añades MySQL
MYSQL_USER=${{MySQL.MYSQLUSER}}
MYSQL_PASSWORD=${{MySQL.MYSQLPASSWORD}}
MYSQL_HOST=${{MySQL.MYSQLHOST}}
MYSQL_PORT=${{MySQL.MYSQLPORT}}
MYSQL_DATABASE=${{MySQL.MYSQLDATABASE}}

# Seguridad - GENERA UNA NUEVA con: python -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=GENERA_UNA_CLAVE_SEGURA_AQUI

# Configuración
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=11520

# Librería nativa (.so para Linux)
DLL_PATH=/app/app/dll/ELECTREIIISL.so
DEBUGGER_PATH=/app/app/dll/

# API
API_V1_STR=/api/v1

# CORS (opcional - añade tus dominios)
BACKEND_CORS_ORIGINS=
